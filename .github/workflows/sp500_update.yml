name: 📊 SP500 Daily Update

on:
  schedule:
    # Exécution tous les jours à 15h30 UTC (10h30 EST - après ouverture des marchés US)
    - cron: '30 15 * * *'
  
  # Permet l'exécution manuelle depuis GitHub
  workflow_dispatch:

jobs:
  update-sp500:
    runs-on: ubuntu-latest
    
    # INDISPENSABLE pour que l'étape "git push" fonctionne
    permissions:
      contents: write
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 🐍 Setup Python
      uses: actions/setup-python@v4
      with:
        # CORRECTION : Passage de 3.9 à 3.10 pour supporter la syntaxe de yfinance
        python-version: '3.10'
        
    - name: 📦 Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas yfinance requests
        
    - name: 🚀 Run SP500 updater
      env:
        WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
      run: |
        python sp500_updater.py
        
    - name: 📤 Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        # Ajout des fichiers (vérifie bien que ces fichiers sont générés par ton script)
        git add sp500_data.csv sp500_info.json
        # Le '|| echo' empêche le workflow de planter s'il n'y a rien à commiter
        git diff --staged --quiet || (git commit -m "📊 Update SP500 data - $(date)" && git push)
        
    - name: 📝 Log completion
      run: |
        echo "✅ SP500 update completed at $(date)"
        echo "📊 Data accessible at: https://raw.githubusercontent.com/${{ github.repository }}/main/sp500_data.csv"
